# 같이가개 신청서 업데이트 (v1.0 → v2.0)

> 이 문서는 「2026년 부산광역시 공공데이터·AI 활용 창업경진대회」 제출용 신청서(v1.0)를
> 전국 단위 공공데이터 확장 + 실제 코드 구현 반영해서 개정하기 위한 자료입니다.
> 원본 PDF는 서식(표/레이아웃)까지 완성된 상태라, 아래 내용을 각 절에 그대로 반영해
> 워드 원본에서 해당 섹션만 교체하시면 됩니다. (현재 세션은 디스크 공간 문제로 .docx를
> 직접 생성하는 빌드 환경을 쓸 수 없어 우선 텍스트로 정리했습니다 — 아래 "일러두기" 참고)

---

## 0. 이번 작업 요약

**1) 코드(pet 폴더) 실제 개선 — 완료**

- 동물병원·동물약국은 상세페이지가 인앱 모달이 아니라 **별도의 새 창(팝업)** 으로 열리도록 변경
- 동물병원 상세페이지에 **진료과목**(세부 전문과 있으면 "심장내과" 등 표기, 없으면 **"종합진료"** 기본 표기), **가능 동물** 필드 추가
- **반려동물 친화도 점수(0~100점, Rule-based)** 실제 계산 로직 구현 — 신청서에 적혀 있던 가중치(리뷰만족도45%·공공데이터검증25%·사용자반응20%·편의시설10%) 그대로 코드화, 상세페이지에 신호등(초록/노랑/빨강) 배지로 표시
- **위치 기반 AI 추천순 정렬** (Content-Based 스코어링: 거리·필터일치도·편의시설·신규등록여부) 구현, 지도 리스트 패널에 "거리순 / AI 추천순" 토글 추가
- 지도 카테고리 필터에 **"💊 동물약국"** 신설 (기존엔 동물병원만 있었음)

**2) 전국 단위 실존 공공데이터셋 조사 — 완료**

data.go.kr(공공데이터포털) 기준으로 실제 존재하는 전국 단위 데이터셋 확인. 아래 2절 표 참고.

**3) 신청서 문구 개정안 — 아래 2~5절**

---

## 일러두기 (중요)

- 이번 세션은 실행 환경(Linux 샌드박스) 디스크 공간 부족으로 `.docx` 파일을 직접 생성하는
  빌드 스크립트(docx-js)를 돌릴 수 없었습니다. 그래서 이 파일은 **Word 서식이 적용된 최종
  신청서가 아니라, 각 섹션에 그대로 붙여넣을 수 있는 개정 문구/표**입니다.
- 다음 세션에서 환경이 정상화되면 원본과 동일한 서식의 `.docx` v2.0을 바로 생성해드릴 수
  있습니다. 원하시면 알려주세요.
- **공모전 적합성 관련 주의사항**: 이 공모전은 "부산광역시" 주최이고 참가자 정보란에
  "부산 공공데이터 활용 및 지역 특성 반영 ■해당"으로 체크되어 있습니다. 데이터셋을 전국
  단위로 완전히 갈아타면 이 체크 항목의 설득력이 약해질 수 있어, **부산 특화 데이터는
  그대로 유지하고 "전국 확장"을 상위 레이어로 얹는 구조**로 개정했습니다. 즉 "이미 전국
  17개 시도 데이터를 처리하는 아키텍처를 구현했고, 그중 부산을 1차 실증 도시로 베타 출시한다"는
  논리입니다 — 실제로 코드(`awsPlaces.ts`)에 부산·서울·경기 등 전국 17개 지역코드 슬롯이
  이미 구현돼 있어 이 주장은 사실에 부합합니다.

---

## 1. "다. 활용 데이터 목록" 개정안 (1.1-다 교체)

기존 11개 → **16개 데이터셋**으로 확장. 부산 특화 5종은 유지하고, 전국 단위 실존 데이터를
6종 추가해 "전국 커버리지 + 부산 하이퍼로컬" 이중 구조로 재편했습니다.

| 분류 | 데이터명 | 제공기관 | 활용 | 업데이트 주기 |
|---|---|---|---|---|
| 전국 기반 | 행정안전부_동물_동물병원 조회서비스 | 행정안전부(공공데이터포털) | 전국 동물병원 인허가·영업상태·소재지, 지도 마커 기본 노출 | API 상시 갱신 |
| 전국 기반 | 행정안전부_동물약국 | 행정안전부(공공데이터포털) | 전국 동물약국 인허가·영업상태, 신규 카테고리(💊) 마커 | 3일 이내 갱신 |
| 전국 기반 | 농림축산검역본부_동물등록 현황 | 농림축산검역본부 | 시군구별 등록 추세 분석, 전국 수요예측 입력값 | 월간 |
| 전국 기반 | 농림축산검역본부_동물보호센터 정보 / 전국동물보호센터정보표준데이터 | 농림축산검역본부 | 전국 유기동물 입양 연계, 보호소 위치 노출 | 수시 |
| 전국 기반 | 농림축산검역본부_국가동물보호정보시스템 구조동물 조회서비스 | 농림축산검역본부 | 전국 유기(구조)동물 공고·주인찾기, 입양 연계 | 실시간 |
| 전국 기반 | 한국문화정보원_전국 반려동물 동반 가능 문화시설 위치 데이터 | 한국문화정보원 | 전국 박물관·문화시설 반려동반 여부, 나들이 추천 | 수시/비정기 |
| 부산 특화 | 부산광역시 반려동물등록현황 | 부산광역시 | 자치구별 양육 밀집도, B2B 상권분석 | 연간 |
| 부산 특화 | 부산광역시 15분 도시공원 | 부산광역시 | 산책 코스 추천 알고리즘 입력값 | 수시/비정기 |
| 부산 특화 | 부산광역시 동물병원 현황 | 부산광역시 | 24시 응급 동선 추천, 마커 기본 노출 | 실시간 반영 |
| 부산 특화 | 부산광역시 119 구조 반려동물 정보 | 부산광역시 | 유기(실종)동물 주인 찾기 홍보 및 입양 연계 | 실시간 반영 |
| 부산 특화 | 부산광역시 동물보호센터 현황 | 부산광역시 | 유기동물 입양 연계 | 연간 |
| 후원기관 | 부산관광공사 반려동물 동반여행 코스 추천 데이터 | 부산관광공사(비짓부산) | 펫 동반 관광 코스, 축제 정보 | 비정기 |
| 신뢰성 검증 | 식품안전나라 반려동물 동반출입 음식점 | 식약처 | 제보 자동 교차검증 | 실시간 DB 기준 |
| 신뢰성 검증 | 식품위생업소 인허가 업소 정보 | 식약처 | 폐업 매장 자동 제외 | 상시/비정기 |
| 전국 확장 | 한국관광공사_반려동물_동반여행_서비스 | 한국관광공사 | 관광지·숙소·음식점 통합, 반려동반 조건 상세 | 실시간 반영 |
| 전국 확장 | 행정안전부_동물_동물판매업 조회서비스 | 행정안전부(공공데이터포털) | 반려동물 분양업소 정보(향후 입양 연계 확장용) | API 상시 갱신 |

**총 16개 데이터셋(부산 특화 5종 + 전국 기반 6종 + 후원기관 1종 + 신뢰성 검증 2종 + 전국 확장 2종)을
융합.** 기존 "1차 부산 수집 → 3단계에서 전국 확장" 로드맵을, **"데이터 아키텍처는 처음부터
전국 대응으로 설계 → 부산을 1차 실증·베타 지역으로 우선 운영"** 구조로 수정.

---

## 2. "마. 부산 지역 특성 반영" 개정 문구 (교체용)

> 부산은 (1) 광역시 중 노령 인구 비율 1위로, 반려동물 동반 외출 시 가까운 공원과 동물병원,
> 편의시설을 연계해 확인할 수 있는 생활형 정보 수요가 크다. (2) 해운대·광안리 등 관광형
> 상권 밀집이라는 인프라를 활용해, 부산관광공사 펫 동반 관광 데이터와 결합 시 강한
> 시너지를 가진다. (3) 자치구별 반려동물 등록 및 생활 인프라 격차가 큰 통계를 기준으로
> 상권분석 B2B 수요가 존재한다.
>
> **같이가개는 데이터 파이프라인을 처음부터 전국 17개 시도 대응으로 설계했다.** 실제로
> 장소 데이터 처리 계층(`awsPlaces.ts`)에는 부산·서울·경기·인천·강원 등 전국 17개
> 지역코드가 이미 슬롯 단위로 구현되어 있어, 신규 지자체 데이터가 추가돼도 시스템 구조
> 변경 없이 바로 흡수할 수 있다. 다만 초기 실증은 (1)~(3)의 이유로 **부산을 1차 베타
> 지역으로 선정**해 부산 16개 자치구 데이터를 우선 정제·검증하고, 이후 전국 동물병원·
> 동물약국·유기동물·문화시설 등 전국 단위 데이터를 순차적으로 서비스에 반영해 전국
> 확장을 가속한다. 즉 "부산에서 검증한 뒤 전국으로 넓히는" 것이 아니라 "전국 대응
> 아키텍처 위에서 부산을 가장 먼저 서비스하는" 구조로, 기술적 확장성과 지역 밀착성을
> 동시에 확보했다.

---

## 3. "1.2) AI 기술 활용" 개정 문구 — 구현 현황 반영

기존 신청서는 4종 AI 모델을 전부 "계획"으로만 서술했습니다. 이번에 **① 친화도 점수와
④ 위치기반 추천은 실제로 코드에 구현**했으므로, 아래처럼 구현 상태를 명시하는 걸
권장합니다(심사에서 "실체가 있는 프로토타입"으로 보일 가능성이 높아집니다).

> **① 반려동물 친화도 점수 산출 (0~100점 점수화) — Rule-based 엔진 구현 완료**
> 방문자 리뷰 키워드 감성 분석, 공공데이터 검증 여부, 사용자 찜/추천/비추천 반응,
> 편의시설 보유 여부를 아래 가중치로 종합 연산해 매장별 친화도 점수를 실시간 산출한다.
> - 리뷰 만족도(45%): 리뷰 텍스트 내 긍/부정 키워드 비율 기반 스코어링
> - 정부 데이터 검증(25%): 공공데이터 출처 장소는 100점, 사용자 제보만 있는 장소는 40점
> - 사용자 반응(20%): 추천/비추천 비율 + 찜 수 가산
> - 편의시설 옵션(10%): 대형견 가능, 펫메뉴, 실내외 동반 범위 가중치 부여
>
> 출력은 신호등 체계(녹색 75점 이상/노랑 50~74점/빨강 50점 미만) 배지와 항목별 세부 점수로
> 상세페이지에 표시된다. **KoBERT 파인튜닝 감성분석 모델은 라벨링 데이터 확보 후 다음
> 단계에서 리뷰 만족도 산출부를 교체 투입할 예정**이며, 현재는 그 전 단계인 규칙 기반
> 엔진으로 동일한 인터페이스를 구현해 두어 모델 교체 시 다른 로직 변경 없이 바로
> 승격 가능한 구조다.
>
> **④ 위치 기반 개인화 맞춤 추천 — Content-Based 스코어링 구현 완료**
> 사용자 현재 위치와의 거리, 선택한 선호 필터(실내/야외/대형견 등)와의 일치 여부,
> 편의시설 보유, 신규 등록(14일 이내) 여부를 종합해 추천 점수를 계산하고, 지도 리스트를
> "AI 추천순"으로 재정렬한다. 협업 필터링(Item2Vec)은 사용자 행동 로그가 충분히 누적된
> 이후 별도 배치 파이프라인으로 고도화할 계획이다.
>
> ② 방문자 리뷰 감성·토픽 분석(KoBERT Fine-tuning)과 ③ 신뢰도 기반 사용자 제보 자동
> 검증은 기존 계획대로 다음 단계 개발 항목으로 유지한다.

---

## 4. "4. 추가 사항 - 나. 현재 구현된 프로토타입 설명" 개정 문구

기존 문구에 아래를 추가/수정하는 것을 권장합니다.

> 상세페이지: 친화도 점수(0~100 + 신호등 배지 + 항목별 세부 점수) + 토픽별 점수 + 영업중
> 배지 + 기본 정보. **동물병원·동물약국은 상세페이지가 지도 화면과 분리된 별도 창(팝업)
> 으로 열리며**, 동물병원은 진료과목(세부 전문과가 있으면 "심장내과" 등으로 표기, 없으면
> "종합진료" 기본 표기)과 가능 동물 정보를 추가로 제공한다.
>
> 지도 리스트: 카테고리 필터에 동물약국(💊)을 신설했고, "거리순 / AI 추천순" 정렬 토글을
> 추가해 사용자가 원하는 기준으로 장소를 탐색할 수 있다.
>
> [추후 추가할 기능] 항목에서 **"친화도 점수 UI(0~100점 + 검증 배지)"는 이번에 구현
> 완료되어 항목에서 제외**하고, 아래를 신규로 추가:
> - 토픽별 레이더 차트(위생/서비스/환경 세부 점수 시각화)
> - KoBERT 파인튜닝 감성분석 모델 교체 투입
> - 동물병원 진료과목·가능 동물 정보의 관리자 편집 UI (현재는 데이터 파이프라인에는
>   필드가 반영돼 있으나, 값을 입력할 관리자 화면은 아직 없음 — 다음 단계 필요)

---

## 5. 코드 변경 파일 목록 (참고용)

- `src/components/KakaoMap.tsx` — 동물병원/약국 별도 창 오픈 로직, 동물약국 필터·이모지,
  거리순/AI추천순 정렬 토글
- `src/app/place/[id]/page.tsx` — 친화도 점수 배지, 동물병원 진료과목·가능 동물 필드
- `src/lib/affinityScore.ts` (신규) — 친화도 점수 계산 엔진
- `src/lib/recommend.ts` (신규) — 위치 기반 추천 스코어링
- `src/lib/awsPlaces.ts` — 전국 데이터 매핑에 `specialty_department`, `treatable_animals`
  필드 추가

---

## 다음 단계 제안

1. Supabase `places` 테이블에 `specialty_department`(text), `treatable_animals`(text)
   컬럼 추가 — 수기 등록 장소(부산 특화분)에도 값을 넣을 수 있도록.
2. 전국 데이터 ETL 파이프라인(Lambda)에 위 6종 신규 전국 데이터셋 연동.
3. 환경이 정상화되면 이 문서 내용을 반영한 `.docx` v2.0 최종본 생성.
